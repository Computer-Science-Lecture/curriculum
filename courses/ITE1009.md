# ITE1009 · C 프로그래밍

| | |
|---|---|
| 수강 | 2015 (1학년) |
| 감사 등급 | **C+** |
| 저장소 | [jiunbae/ITE1009](https://github.com/jiunbae/ITE1009) |
| 코드 감사 | [리뷰 전문](../reviews/ITE1009.html) |

## 한 줄 판정

두 개의 레포가 한 디렉터리에 섞여 있다. 하나는 남의 책을 베껴 쓴 학습 기록이고, 다른 하나는 1학년이 혼자 만든 1,500줄짜리 Win32 음악 리스트 앱이다. 평가받아야 할 것은 후자뿐이다.

## 이 과목이 실제로 요구한 것

<!-- 과제 목록과 각 과제가 검증하려 한 개념. 명세 기준으로 작성. -->

## 내가 빠진 함정

<!-- 감사 보고서에서 자동 추출한 초안. 후배가 똑같이 밟을 만한 것만
     남기고, 각 항목에 '왜 그렇게 짰는지'와 '어떻게 알아챌 수 있었는지'를 덧붙이세요. -->

- **[치명적] 날짜 디렉터리의 C 코드는 대부분 Learn C the Hard Way 예제이며, 출처가 어디에도 적혀 있지 않다**  
  `15-11-09/hashmap_algos.c:1–2`  
  ex1.c 부터 ex22_main.c 까지가 책의 예제 번호를 그대로 쓴다. 내용도 그대로다 — ex7.c 는 저자 이름 자리에 "Zed" "Shaw"를 넣어 출력하고, ex12.c 는 "Zed A. Shaw" 를 문자열로 들고 있으며, ex22_main.c:3 은 const char *MY_NAME = "Zed A. Shaw"; 다. ex17.c:117 …
- **[치명적] XML 라이터가 닫는 태그를 전부 </name> 으로 쓴다 — 레포에 커밋된 데이터 파일 3개 전부가 XML이 아니다**  
  `Assignment#4/musicList/main.cpp:280–283`  
  musicListSave 는 <name> 만 제대로 닫고, <album> · <artist> · <genre> 세 줄은 복사·붙여넣기 상태 그대로 </name> 으로 닫는다. 파일 첫 줄에는 <?xml version="1.0" encoding="UTF-8"?> 를 당당히 박아 놓는다.
- **[치명적] 장르 뷰의 필드 순서가 "불러오기/삭제"와 "추가"에서 서로 다르다**  
  `Assignment#4/musicList/main.cpp:230–232 (불러오기) vs :457–459 (추가)`  
  genreList 는 별도 타입을 만들지 않고 musicInfo 의 필드 자리를 바꿔 끼워 재사용한다. 파일에서 읽을 때( :232 )는 (name, genre, album, artist) 순으로 섞어 넣고, 삭제할 때( :438 , :567 )도 같은 순서로 섞어 찾는다. 여기까지는 일관적이다.
- **[치명적] 한 번도 컴파일되지 않은 파일이 3주 간격으로 두 번 커밋됐다 — 두 사본은 바이트 단위로 동일하다**  
  `15-11-25/list.h:11, :29–30, :52`  
  15-11-03/list.cc 와 15-11-25/list.cc 를 diff 로 비교하면 차이가 없다. list.h 도 동일하다. 2015-11-03에 커밋된 파일이 2015-11-25 커밋(" 2015-11-25 ")에서 한 글자도 수정되지 않은 채 새 디렉터리로 복사됐다. 이 두 커밋이 레포의 마지막 실습 기록이다.
- **[중대] table<T>::remove 는 문법적으로 컴파일되지 않는 코드다 — 아무도 호출하지 않아서 들키지 않았다**  
  `Assignment#4/musicList/table.h:100–109`  
  table.h:106–109 의 본문은 세미콜론도 return 도 없고, 멤버 함수 find 를 호출하지 않고 이름만 값으로 넘긴다. 템플릿이라 실체화되기 전까지는 파싱만 통과한다. remove(size_t) 가 이것을 호출하지만 remove(size_t) 자체를 프로젝트 어디서도 부르지 않는다 — grep 으로 확인한 유일한 호출 지점은 table.h:1…
- **[중대] indexTable 은 vector 내부를 가리키는 댕글링 포인터 맵이다**  
  `Assignment#4/musicList/table.h:44–51, :80`  
  생성자와 insert 가 &(this->raw.back()) , 즉 vector<T> 원소의 주소를 unordered_map<size_t, T*> 에 저장한다. push_back 은 용량 초과 시 버퍼를 재할당하므로, 두 번째 insert 부터 이전에 저장한 포인터가 전부 무효가 된다. WM_CREATE 에서 topIcon · botIcon 에 각각 4개씩…

<details>
<summary>그 외 6건 (보고서 참조)</summary>

- [중대] 괄호 없는 abs 매크로 때문에 스크롤 감속이 한쪽 방향에서만 동작한다
- [중대] 메시지 루프가 PeekMessage 로 CPU 코어 하나를 계속 태운다
- [중대] 파일 다이얼로그를 열 때마다 객체가 새고, 파서 버퍼도 해제되지 않는다
- [중대] 파일이 조금만 어긋나면 args[0..3] 이 범위 밖을 읽는다
- [중대] find_index 가 "없음"과 "0번째"를 같은 값으로 반환한다 — 없는 값을 찾으면 "1번째에 있음"이라고 답한다
- [중대] 존재하지 않는 노드 뒤에 삽입하면 즉시 SIGSEGV — 재현됨

</details>

## 먼저 봤으면 좋았을 자료

<!-- 교재/논문/레퍼런스 구현. -->
