# ITE4005 · 데이터 사이언스

| | |
|---|---|
| 수강 | 2018 (4학년) |
| 감사 등급 | **B** |
| 저장소 | [jiunbae/ITE4005](https://github.com/jiunbae/ITE4005) |
| 코드 감사 | [리뷰 전문](../reviews/ITE4005.html) |

## 한 줄 판정

네 개의 데이터 마이닝 알고리즘이 모두 동작하고 세 개는 정답과 대조한 흔적이 남아 있다. 문제는 코드가 아니라 코드에 대한 설명이다.

## 이 과목이 실제로 요구한 것

<!-- 감사 보고서의 과제별 판정표에서 가져온 초안입니다. 강의 명세 PDF는 한글 폰트가
     임베디드 인코딩이라 본문 추출이 불가능해 자동으로 채우지 못했습니다.
     각 과제가 '무엇을 검증하려 했는지'를 직접 덧붙이세요. -->

| 과제 | 주제 | 핵심 판정 | 등급 |
|---|---|---|---|
| A1 | Apriori | 출력 정확·재현됨. 단 downward closure 가지치기가 없어 join 단계만 구현됨 (k=4에서 후보 455개 중 23개만 유효) | B+ |
| A2 | Decision Tree | 트리 자체는 95% 정확. 그러나 random forest는 오타로 죽었고, min_gain 의미가 문서와 반대이며, 커밋된 결과 파일이 손상됨 | C+ |
| A3 | DBSCAN | core/border/noise 판정 정확, 정답과 Jaccard 0.97–1.00. 이웃 탐색은 순수 파이썬 O(n²) (8,000점에 86초), 노이즈 없는 입력에서 KeyError | B |
| A4 | Recommender (SVD) | 누수 없음, cold-start 처리 검증됨, RMSE 재현됨. 그러나 베이스라인 비교가 없고 setup.py에 Surprise 라이브러리 흔적이 그대로 남음 | B |

## 내가 빠진 함정

<!-- 감사 보고서에서 자동 추출한 초안. 후배가 똑같이 밟을 만한 것만
     남기고, 각 항목에 '왜 그렇게 짰는지'와 '어떻게 알아챌 수 있었는지'를 덧붙이세요. -->

- **[치명적] 오타 하나로 random forest가 통째로 죽었다 — 그것도 동작하던 코드를 갈아엎으면서**  
  `assignment2/dt.py:41–46`  
  calssifier 는 classifier 의 오타다. 파이썬은 이 줄에서 새 지역 변수를 만들 뿐 에러를 내지 않는다. 따라서 --forest N 을 주면 RandomForest 객체가 생성되고 즉시 버려지며, 바로 아래 :46 은 :41 에서 만든 단일 DecisionTree를 학습시킨다. lib/forest.py 54줄 전체가 도달 불가능한 죽은 코드…
- **[치명적] min_gain 은 information gain이 아니라 불순도다 — 의미가 문서와 정반대고, 기본값 0.3이 error 메트릭을 무력화한다**  
  `assignment2/lib/tree.py:47, 68`  
  _gain 은 이름과 달리 가중 불순도의 부호를 뒤집은 값 이다. 분할 선택에서는 이게 맞다(argmax = 불순도 최소). 그런데 :68 의 조기 종료 조건은 -_gain([left,right]) , 즉 자식 노드의 가중 불순도를 min_gain 과 비교한다. 부모 불순도에서 빼는 과정이 없으므로 이건 information gain이 아니다.
- **[중대] Apriori의 가지치기 단계가 없다. join만 하고 downward closure 검사를 건너뛴다**  
  `assignment1/apriori.py:29–33`  
  Apriori 알고리즘의 핵심은 두 단계다. (1) 빈발 (k-1)-항목집합을 조인해 k-후보를 만들고, (2) 그 후보의 모든 (k-1)-부분집합이 빈발한지 확인해 아닌 것을 버린다. 이 코드에는 (1)만 있다. 두 개의 빈발 (k-1)-집합의 합집합이 크기 k이기만 하면 무조건 후보가 된다.
- **[중대] 출력이 실행할 때마다 달라진다**  
  `assignment1/apriori.py:60`  
  frozenset 순회 순서는 파이썬 해시 시드에 의존한다. ','.join(item) 이 그 순서를 그대로 찍기 때문에 같은 입력·같은 코드로 두 번 돌리면 {16,8} 과 {8,16} 처럼 다른 파일이 나온다. 실제로 확인했다 — 두 번 실행한 1,066줄 출력에서 908줄이 서로 다른 텍스트 였다. 규칙 집합 자체는 동일하다(원소 순서를 정렬하면 완전…
- **[중대] 제출된 결과 파일이 손상돼 있다 — yes 가 ye 로 잘렸다**  
  `assignment2/data/dt_result.txt:3 (vs test/dt_answer.txt:3)`  
  assignment2/data/dt_result.txt 의 3~5행 클래스 값이 ye 다. 줄바꿈 문제가 아니다. 헥스 덤프로 확인했다: 66 61 69 72 09 79 65 0a = "fair	ye
" . 문자 하나가 실제로 없다.
- **[중대] 보고서의 95.66%는 커밋된 코드로 재현되지 않는다. 노트북 프로토타입의 숫자다**  
  docs/Decision Tree.md:75 는 dataset1에서 Entropy 95.66%를 보고한다. 커밋된 data/dt_result1.txt 를 정답과 대조하면 정확히 331/346 = 95.66%가 나온다. 그런데 같은 디렉터리의 test/dt_result1.txt 는 327/346 = 94.51% 이고, 현재 dt.py 를 기본 인자로 돌리면 …

<details>
<summary>그 외 6건 (보고서 참조)</summary>

- [중대] 이웃 탐색이 전수 스캔이다. 8,000점에 86초
- [중대] 노이즈가 하나도 없는 입력에서 KeyError로 죽는다
- [중대] 시간 측정 모듈이 아무것도 재지 않는다
- [중대] 베이스라인 비교가 없다. 100차원 잠재 인수가 사주는 것은 2.7%다
- [중대] SGD 갱신이 표준형과 다르다. 그리고 그 사실을 저자도 알고 있었던 흔적이 있다
- [중대] setup.py 에 Surprise 라이브러리의 흔적이 그대로 남아 있고, 출처 표기가 없다

</details>

## 먼저 봤으면 좋았을 자료

<!-- 교재/논문/레퍼런스 구현. -->
