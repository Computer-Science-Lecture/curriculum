# ITE4065 · 병렬 프로그래밍

| | |
|---|---|
| 수강 | 2017 (3학년) |
| 감사 등급 | **B-** |
| 저장소 | [jiunbae/ITE4065](https://github.com/jiunbae/ITE4065) |
| 코드 감사 | [리뷰 전문](../reviews/ITE4065.html) |

## 한 줄 판정

야심은 상위 5%, 검증은 하위 50%. 네 프로젝트 모두 “돌아간다”를 “정확하다”의 증거로 받아들였고, 병렬 프로그래밍은 그 둘이 가장 크게 어긋나는 분야다.

## 이 과목이 실제로 요구한 것

<!-- 과제 목록과 각 과제가 검증하려 한 개념. 명세 기준으로 작성. -->

## 내가 빠진 함정

<!-- 감사 보고서에서 자동 추출한 초안. 후배가 똑같이 밟을 만한 것만
     남기고, 각 항목에 '왜 그렇게 짰는지'와 '어떻게 알아챌 수 있었는지'를 덧붙이세요. -->

- **[치명적] 제출 브랜치(master)에 스레드가 0개다**  
  `project1/README.md· 저자 본인의 기록`  
  project1/src/main.cc 는 순수 싱글스레드다. include/pool.h 에 스레드풀이 존재하지만 main.cc 어디서도 사용되지 않는다. 병렬 프로그래밍 과목의 첫 과제에서 기본 브랜치가 순차 코드인 것은 변명의 여지가 없다. origin/multi 브랜치에 병렬 버전이 있으나, 채점자가 master 를 본다면 그것은 없는 것과 같다.
- **[치명적] Aho-Corasick이 아니다**  
  `project1/include/ahocorasick.h:45–68`  
  README는 - [x] make Failure Link and Output Link 로 체크되어 있지만, include/ahocorasick.h 어디에도 failure link를 구성하는 코드가 없다. match() 는 모든 시작 위치마다 루트에서 트라이를 다시 타는 완전 탐색이다. 복잡도는 Aho-Corasick의 O(|q| + matches)가 아니라…
- **[치명적] 데드락 탐지가 실행되지 않는다**  
  `project2/include/container.hpp:328–350`  
  330행의 return true 때문에 그 아래 wait-for 그래프 구성과 BFS 사이클 탐지 전체가 도달 불가능한 코드 다. 실제 동작은 “데드락 탐지”가 아니라 “충돌하면 무조건 abort” 다. 과제의 핵심 요구사항이 구현되어 있지 않다.
- **[치명적] 연쇄 결과 — README가 자랑한 RW 뮤텍스가 한 번도 대기하지 않는다**  
  락 보유자는 항상 waiting[rid] 에 엔트리를 남긴다. 따라서 try_acquire 실패는 곧 waiting[rid] 가 비어 있지 않음을 뜻하고, 위 결함에 의해 모든 충돌이 예외 없이 abort 된다. 그 결과 블로킹 acquire() 로 가는 try_failed 분기도 도달 불가능해진다.
- **[치명적] 그 뮤텍스 자체도 깨져 있다**  
  `project2/include/mutex.hpp:36–42, 50–62`  
  try_lock() (mutex.hpp:50)은 FIFO 순서를 무시하고 waiter 큐 뒤에 자신을 넣으면서 writing = true 로 만든다. 반면 unlock() 은 “첫 번째 writer 엔트리”를 지운다. 따라서 대기 중인 다른 writer의 엔트리를 지울 수 있고 , 그 writer는 영원히 만족될 수 없는 조건을 기다린다.
- **[치명적] atomic::Snapshot 에 std::atomic 이 없다**  
  `project3/include/snapshot.hpp:117–121, 78–82`  
  이 알고리즘의 정확성 증명은 “레지스터의 읽기/쓰기가 원자적이고 순서가 보인다” 를 전제한다. 코드에는 그 전제가 없다. x86에서 정렬된 포인터 저장이 찢어지지는 않지만, 메모리 배리어도 volatile 도 없어 -O3 에서 컴파일러가 객체 초기화와 저장을 재배열하거나 table[i] 로드를 루프 밖으로 끌어낼 수 있다. C++ 표준상으로는 그냥 data…

<details>
<summary>그 외 17건 (보고서 참조)</summary>

- [치명적] moved 배열이 스레드 간 공유다
- [치명적] 벤치마크가 스냅샷 성능을 측정하지 않는다
- [치명적] 충돌 검사가 존재하지 않는다
- [치명적] 락 객체가 “어떤 레코드”인지 모른다
- [치명적] 힙 오버플로우 — 포인터 크기만 할당하고 구조체로 쓴다
- [치명적] GC가 사실상 동작하지 않는다 — 타임스탬프 부여 시점 버그
- [중대] 패턴 갱신이 전체 재구축이다
- [중대] multi 브랜치는 병렬화 축이 틀렸다
- [중대] abort 경로마다 새는 메모리와 남는 dangling pointer
- [중대] 커밋 카운터가 초기화되지 않았고 락 없이 읽힌다
- [중대] 설계 차원의 아이러니 — 전역 뮤텍스
- [중대] “왜 코어 많은 서버가 더 느린가”의 답
- [중대] 메모리 회수가 “1000개면 괜찮겠지”다
- [중대] 컴파일러가 지워버릴 수 있는 스핀
- [중대] 성능 해석이 낙관적이다 — 64스레드 이하에서는 더 느리다
- [중대] MariaDB 전체 트리를 그대로 커밋했다
- [중대] 라이선스 충돌

</details>

## 먼저 봤으면 좋았을 자료

<!-- 교재/논문/레퍼런스 구현. -->
