# CSE4006 · 소프트웨어공학

| | |
|---|---|
| 수강 | 2017 (3학년) |
| 감사 등급 | **C** |
| 저장소 | [jiunbae/CSE4006](https://github.com/jiunbae/CSE4006) |
| 코드 감사 | [리뷰 전문](../reviews/CSE4006.html) |

## 한 줄 판정

설계 감각은 확실히 있는 사람이 만든 레포다. 다만 "테스트를 모두 통과했으므로 올바르게 작동한다"는 보고서의 핵심 주장이, 애초에 실행될 수 없는 테스트 위에 서 있다.

## 이 과목이 실제로 요구한 것

<!-- 과제 목록과 각 과제가 검증하려 한 개념. 명세 기준으로 작성. -->

## 내가 빠진 함정

<!-- 감사 보고서에서 자동 추출한 초안. 후배가 똑같이 밟을 만한 것만
     남기고, 각 항목에 '왜 그렇게 짰는지'와 '어떻게 알아챌 수 있었는지'를 덧붙이세요. -->

- **[치명적] 과제의 절반인 lock-free BST가 아예 없다**  
  `ParBST/README.md:49`  
  hw3 Part 2는 lock-free 자료구조 두 개 를 요구한다. 명세 본문은 "LOCK-FREE (SORTED) LINKED LIST를 구현하고, LOCK-FREE BINARY SEARCH TREE를 구현한다. LOCK-FREE BST는 LEAF-ORIENTED BST …"로 이어지고, 제출 지침은 리포지토리를 LF_LL (LINKED-LIST) 과 …
- **[치명적] 없는 키를 지우면 NPE가 나면서 트리 전역 락이 영구히 잠긴다**  
  `ParBST/src/collections/concurrent/BinaryTree.java:104–118`  
  delete() 는 루트의 자식으로 내려갈 때 null 검사를 하지 않는다. 트리에 {5} 만 있는 상태에서 delete(3) 을 호출하면 cur = cur.left 가 null 이 되고 바로 cur.lock() 에서 NullPointerException 이 난다.
- **[치명적] 탐색이 실패하면 노드 락을 반납하지 않고 빠져나온다**  
  `ParBST/src/collections/concurrent/BinaryTree.java:137–147`  
  delete() 의 탐색 루프는 대상을 못 찾고 리프를 지나칠 때 return false 로 빠져나가는데, 그 시점에 par 가 잡고 있는 락을 풀지 않는다.
- **[치명적] RWBinaryTreeTest 는 트리를 생성하지 않는다 — RW 트리는 한 번도 검증된 적이 없다**  
  `ParBST/test/collections/concurrent/RWBinaryTreeTest.java:13–36`  
  보고서가 Part 1에서 가장 공들여 설명하는 것이 read-write lock 버전이다. 커스텀 OrderedLock 을 새로 만든 이유, downgrade() 를 추가한 이유, dead lock을 만난 경위까지 세 문단을 쓴다. 그런데 그 구현의 테스트 클래스는 @Before 에서 pool 만 만들고 tree 는 만들지 않는다. 필드 선언(14행)만 있…
- **[치명적] read → write 락 승격이 원자적이지 않다 — 보고서가 해결했다고 주장하는 바로 그 문제**  
  `ParBST/src/collections/concurrent/RWBinaryTree.java:79–89`  
  보고서는 downgrade() 를 만든 이유를 이렇게 설명한다: "WriteLock.unlock(), ReadLock.lock()과 같이 구현하게 되면 Write Lock을 해제하고 Read Lock을 잡기 전에 다른 스레드가 Write Lock을 잡아버릴 위험이 있기에 … 하향시키는 기능을 추가하였습니다." 내려오는 방향(write→read)은 실제로 d…
- **[치명적] 워커 스레드에서 터진 AssertionError 는 아무 데도 도달하지 않는다 — 테스트가 실패할 수 없다**  
  `LF_LL/src/concurrent/Pool.java:65–69`  
  LinkedListTest.contains (53행)와 BinaryTreeTest.searchParallel (75행)은 JUnit 단언을 워커 스레드 안에서 실행한다. assertTrue 가 실패하면 AssertionError 가 던져지는데, 이것은 Error 이지 RuntimeException 이 아니다. Pool.Worker.run() 의 catch …

<details>
<summary>그 외 14건 (보고서 참조)</summary>

- [치명적] getDistance 는 BFS 레벨이 아니라 "몇 번째로 꺼냈는가"를 거리로 반환한다
- [치명적] 17번째 사람을 추가하면 ArrayIndexOutOfBoundsException 이 난다
- [치명적] 인접 리스트 "확장"이 배열을 오히려 줄인다
- [중대] 문서화된 재시도 계약을 호출자가 지키지 않는다
- [중대] RWBinaryTree.delete() 가 없는 키에 대해 true 를 반환한다
- [중대] inOrderHelper 는 어떤 노드의 락도 풀지 않는다
- [중대] attemptMark 는 마크를 검사하지 않는다 — 두 스레드가 같은 노드 삭제에 동시에 "성공"한다
- [중대] size() 는 무슨 일이 있어도 0을 반환한다
- [중대] 정렬 기준이 compareTo 가 아니라 hashCode 다
- [중대] 성능 로그의 숫자가 자기 자신과 12,000배 모순되는데, 보고서는 그 숫자를 해석한다
- [중대] join() 이 대기 중인 워커를 깨우지 않고, terminate 는 volatile 이 아니다
- [중대] 원형 큐가 pop마다 배열을 재할당해 BFS를 O(V²)로 만든다
- [중대] clone() 이 복제가 아니라 자기 자신을 변형한다
- [중대] "skeleton을 변경하지 않았다"는 보고서 주장과 실제 커밋이 다르다

</details>

## 먼저 봤으면 좋았을 자료

<!-- 교재/논문/레퍼런스 구현. -->
